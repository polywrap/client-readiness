import PolywrapClient
import Foundation

struct ConfigPluginPackageTest: Feature {
    func runTestCase(input: Any) throws -> Void {
        fatalError("Inline plugin not supported in swift")
    }
}

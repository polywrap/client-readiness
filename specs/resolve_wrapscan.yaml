required: true
cases:
  wrapscan:
    input: "wrap://wrapscan/polywrap/uri-resolver@1.0"
    output:
      stdout:
        - "$Test Start [resolve_wrapscan.wrapscan]"
        - "URI Authority: wrapscan"
        - "Resolving: wrap://wrapscan/polywrap/uri-resolver@1.0"
        - "Received: package"
        - Success!

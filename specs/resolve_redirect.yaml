required: true
cases:
  sanity:
    input:
      from: wrap://uri/a
      to: wrap://uri/b
    output:
      stdout:
        - "$Test Start [resolve_redirect.sanity]"
        - Resolving Redirect
        - Received URI 'wrap://uri/b'
        - Success!

required: true
cases:
  sanity:
    input:
      directory: $ROOT/wraps/numbers-type/implementations/as
      uri: "wrap://embed/wrap"
    output:
      stdout:
        - "$Test Start [resolve_instance.sanity]"
        - "Resolving URI: wrap://embed/wrap"
        - "Received: wrapper"
        - Success!
